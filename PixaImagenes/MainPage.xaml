<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PixaImagenes"
             xmlns:cells="clr-namespace:PixaImagenes.Cells;assembly=PixaImagenes"
             x:Class="PixaImagenes.MainPage">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <SearchBar x:Name="ImageSearchBar" SearchButtonPressed="ImageSerachBarSearchButtonPressed"></SearchBar>

        <ListView Grid.Row="1" x:Name="ImageList" RowHeight="100"></ListView>
    </Grid>
</ContentPage>